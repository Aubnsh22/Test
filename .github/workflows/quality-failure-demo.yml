name: Quality Failure Demo

on:
  workflow_dispatch:

jobs:
  regression-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run notification isolation regression test
        run: |
          echo "Running repository notification isolation checks..."
          echo "Expected recipient scope to match the current projectId."
          echo "Regression detected: stopping the quality gate."
          exit 1
